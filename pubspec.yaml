name: flutter_bottom_navigation
description: A simple wrapper for flutter's BottomNavigationBar that handles the current index for you.
version: 1.0.2
repository: https://github.com/mj12358/flutter-bottom-navigation

environment:
  sdk: ">=2.18.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  dart_lintz: ^1.0.0
  flutter_test:
    sdk: flutter
