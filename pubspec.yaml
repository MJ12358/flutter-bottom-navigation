name: flutter_bottom_navigation
description: A simple wrapper for flutter's BottomNavigationBar.
version: 1.0.0
repository: https://github.com/mj12358/flutter-bottom-navigation

environment:
  sdk: ">=2.18.4 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
